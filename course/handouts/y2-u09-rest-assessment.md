# Exit ticket — REST: leggere lo stato

Obiettivo: consumare una risorsa JSON.

1. So spiegare che cosa rappresenta una risorsa?
2. So distinguere HTTP da stile REST?
3. Il mio client rifiuta response incomplete?

4. Allega un'evidenza dell'esercizio base: Leggi `/api/status` e verifica il contratto minimo.
5. Descrivi un errore osservato e il cleanup eseguito. Il marker da solo non vale come evidenza.
