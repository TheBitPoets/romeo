# Exit ticket — Costruisci una API FastAPI

Obiettivo: definire un endpoint tipizzato.

1. So indicare quale riga registra la route?
2. So spiegare chi converte il dizionario in JSON?
3. So trovare la route nello schema OpenAPI?

4. Allega un'evidenza dell'esercizio base: Implementa `GET /status` con risposta tipizzata e testala.
5. Descrivi un errore osservato e il cleanup eseguito. Il marker da solo non vale come evidenza.
